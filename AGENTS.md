# 提交规范

遵循约定式提交规范，英文内容；需要**客观描述**需要提交的文件的所有 Diff 详情，不要通过文件名称猜测，不要通过上下文推断。

# 前端包管理器

`ProcurementSystem.Vue` 前端项目统一使用 **pnpm** 作为包管理器，禁止使用 npm/yarn 安装依赖或执行脚本（如 `pnpm install`、`pnpm build`、`pnpm add` 等）。

# 注释风格

**核心原则**：解释“为什么”，而非“做什么”。

**禁止**：
- 重复代码本身的废话
- 过时或误导的注释
- 注释掉的大段废弃代码
- “日记式”注释（署名、日期）
- 装饰性分隔线（`----`、`====`、`── ──` 等）
